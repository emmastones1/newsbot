name: Forex News Bot

on:
  schedule:
    - cron: '*/5 * * * *'   # every 5 minutes, UTC — tighter grid so odd-minute
                             # news release times (e.g. :15, :45) aren't missed
  workflow_dispatch:
    inputs:
      force_send:
        description: 'Send everything immediately, ignoring schedule (for testing)'
        type: boolean
        default: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot check
        run: python3 forex_news_bot.py
        env:
          FF_BOT_TOKEN: ${{ secrets.FF_BOT_TOKEN }}
          FF_CHAT_ID: ${{ secrets.FF_CHAT_ID }}
          FF_LOCAL_TZ: Africa/Lagos
          FF_DIGEST_TIME: '06:00'
          FF_ALERT_MINUTES: '30'
          FF_FORCE_SEND: ${{ inputs.force_send }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CF_KV_NAMESPACE_ID: ${{ secrets.CF_KV_NAMESPACE_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
